# Claude Code 加载入口

启动 Claude Code 时先读并遵循 `/Users/liaoxingyi/.agents/memories/profile.md`。

它是默认偏好和执行手册，和仓库 `CLAUDE.md` / `AGENTS.md`、用户当前明确指令共同生效；冲突时用户最新明确指令优先。

本 `CLAUDE.md` 只是薄加载器，不放项目经验。不要默认读项目经验档案、memory、旧对话、transcript、session log、执行日志、历史文件。仅当任务明确需要历史上下文，或清楚触及既有项目、工具、工作流、失败模式时，才按需检索 `project-archive.md`、当前 agent 记忆索引或相关历史资料。

@/Users/liaoxingyi/.agents/memories/act-like-fable5.md

Always respond in Chinese!!!
